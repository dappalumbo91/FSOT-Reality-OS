[package]
name = "reality_os_scalar"
version = "0.1.0"
edition = "2021"
description = "FSOT Reality OS — no_std scalar engine S=K(T1+T2+T3), pin D1D38A seeds"
license = "MIT OR Apache-2.0"

[features]
default = []
std = []

[dependencies]
libm = "0.2"
