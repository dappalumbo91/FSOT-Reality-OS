[package]
name = "reality_os_mem"
version = "0.2.0"
edition = "2021"
description = "FSOT Reality OS — frame allocator + heap on mapped physical memory"
license = "MIT OR Apache-2.0"

[dependencies]
bootloader = { version = "0.9.35", default-features = false, features = ["map_physical_memory"] }
