# Architecture

```text
┌─────────────────────────────────────────────────────────┐
│  Reality OS host kernel (this process)                  │
│  scripts/reality_os_cli.py → reality_os.core            │
├─────────────────────────────────────────────────────────┤
│  Layers                                                 │
│   residual.py     c = m(1+|S|f)  preregistered f        │
│   matter_*        conjugate duals, η, Ω_b               │
│   quantum_trinary entanglement structure + trit syntax  │
├─────────────────────────────────────────────────────────┤
│  engine/fsot_compute.py   pin D1D38A                    │
│   seeds → L1/L2 → T1+T2+T3 → S                          │
│   35 DomainConfig interfaces                            │
├─────────────────────────────────────────────────────────┤
│  Future: Linux userspace services / kernel hooks        │
│  Future: Rust no_std scalar kernel → QEMU / bare metal  │
└─────────────────────────────────────────────────────────┘
```

**One formula fabric.** Domains are dimensional interfaces, not separate physics engines.
